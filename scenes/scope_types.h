#pragma once

typedef enum {
    ScopeViewVariableItemList,
    ScopeViewSubmenu,
    ScopeViewWidget,
    ScopeViewSave,
    ScopeViewStart,     // Custom main-menu view (Run + mode selector + description)
} ScopeView;
